{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "Allow the main window to check for and install desktop app updates, then relaunch.",
  "platforms": ["macOS", "linux", "windows"],
  "windows": ["main"],
  "permissions": ["updater:default", "process:allow-restart"]
}
