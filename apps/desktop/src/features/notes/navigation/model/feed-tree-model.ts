// Compatibility seam for desktop callers. The date hierarchy is platform-free
// so mobile can reuse the exact same Feed grouping when its UI adopts it.
export * from "@typenotes/shared/feed-tree-model";
