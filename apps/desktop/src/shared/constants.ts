// Desktop-app constants. Domain constants shared with mobile (system folder
// names, isSystemFolder) live in @typenotes/shared/constants.
export const indentationWidth = 18;

export const PROFILE_SYNC_STORAGE_KEY = "notes-viewer-profile-sync-settings";

// 16px is the base the skin's leading (1.75) and 720px measure are tuned for;
// anyone who has already picked a size keeps it, this only moves the default.
export const DEFAULT_EDITOR_FONT_SIZE = 16;
export const MIN_EDITOR_FONT_SIZE = 12;
export const MAX_EDITOR_FONT_SIZE = 28;
